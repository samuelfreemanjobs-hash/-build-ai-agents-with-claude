---
name: scope-intake
description: Extracts architecture scope — system boundary, views needed, as-is/to-be horizon, tier. Use at S0.
---

# Scope Intake

Extract: system name, boundary, views (as-is, to-be, both), stakeholders,
tier triggers. Output: `architecture-scope.schema.json`. HALT if system
boundary undefined.
